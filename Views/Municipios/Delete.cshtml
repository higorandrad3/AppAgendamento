@model MunicipiosViewModel

@{
    ViewData["Title"] = "Excluir Município";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h2 class="mb-0 text-danger">Excluir Município</h2>
    <a asp-action="Index" class="btn btn-secondary">Voltar</a>
</div>

<div class="alert alert-danger">
    Tem certeza de que deseja excluir este município?
    <strong>Essa ação não poderá ser desfeita.</strong>
</div>

<hr />

<div class="card shadow-sm">
    <div class="card-body">

        <dl class="row">

            <dt class="col-sm-3">Nome</dt>
            <dd class="col-sm-9">@Model.Nome</dd>
        </dl>

    </div>
</div>

<form asp-action="DeleteConfirmed" method="post" class="mt-4">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="btn btn-danger px-4">
        Confirmar Exclusão
    </button>
    <a asp-action="Index" class="btn btn-secondary ms-2 px-4">Cancelar</a>
</form>
